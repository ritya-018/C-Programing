#include<stdio.h>
int main()
{
    int n;
    printf("enter the given no:");
    scanf("%d",&n);
    if(n>0)
    {
        printf("positive no:");
    }
    else
    {
        printf("not positive no");
    }
    return 0;
}